export type Theme = {
  background: string;

  baseFont: {
    font: string;
    size: string;
    color: string;
    lineHeight: string;
  };

  colors: {
    ccent: string;
    button: string;
  };

  components: {};
};
